<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>ZeroWaste - Connecting Food Donors with Receivers</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>

  <style>
    :root {
      --primary: #4CAF50;
      --primary-dark: #2E7D32;
      --primary-light: #C8E6C9;
      --accent: #8BC34A;
      --white: #ffffff;
      --light-gray: #f5f5f5;
      --text: #333333;
    }

    body {
      font-family: 'Poppins', sans-serif;
      color: var(--text);
      margin: 0;
      padding: 0;
      padding-top: 70px;
    }

    header.d-flex {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1000;
      background: transparent;
      backdrop-filter: blur(4px); /* subtle blur for readability */
     
    }

    header .nav-link, 
    header .fs-4 {
      color:  !important;
    }

    header .nav-link:hover {
      color: var(--primary-light) !important;
    }

    .nav-link{
      gap:10px;
    }

   

    .hero-section {
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: url('https://images.pexels.com/photos/6590928/pexels-photo-6590928.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2');
      background-size: cover;
      background-position: center;
      padding: 20px;
    }

    .quote-container {
      text-align: center;
      max-width: 800px;
      padding: 40px;
      margin: 0 auto;
    }

    .quote {
      font-size: 2.5rem;
      font-weight: 600;
      color: var(--white);
      margin-bottom: 30px;
      line-height: 1.3;
      text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
    }

    .author {
      font-size: 1.3rem;
      font-style: italic;
      color: var(--white);
      opacity: 0.9;
      margin-bottom: 40px;
      text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
    }

    .login-options {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin-top: 50px;
      flex-wrap: wrap;
    }

    .login-card {
      width: 220px;
      height: 220px;
      background: rgba(255, 255, 255, 0.9);
      border-radius: 15px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 25px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.12);
      transition: all 0.4s ease;
      cursor: pointer;
      text-decoration: none !important;
    }

    .login-card:hover {
      transform: translateY(-10px) scale(1.05);
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
      background: var(--white);
    }

    .login-card.donor {
      border-top: 5px solid var(--primary);
    }

    .login-card.receiver {
      border-top: 5px solid var(--accent);
    }

    .login-icon {
      font-size: 3rem;
      margin-bottom: 20px;
      color: var(--primary);
    }

    .login-card.receiver .login-icon {
      color: var(--accent);
    }

    .login-title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 10px;
      color: var(--text);
    }

    .login-desc {
      font-size: 0.9rem;
      color: #666;
    }

    @media (max-width: 768px) {
      body {
        padding-top: 60px;
      }

      .quote {
        font-size: 1.8rem;
      }

      .author {
        font-size: 1.1rem;
      }

      .login-options {
        flex-direction: column;
        align-items: center;
      }

      .quote-container {
        padding: 20px;
      }
    }
  </style>

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet"/>
</head>
<body>

  <!-- Navbar -->
  <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
    <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
      <svg class="bi me-2" width="40" height="32" aria-hidden="true"><use xlink:href="#bootstrap"></use></svg>
      <span class="fs-4">ZeroWaste</span>
    </a>

    <ul class="nav nav-pills">
      <li class="nav-item"><a href="/login"  class="nav-link ">Donars</a></li>
      <li class="nav-item"><a href="/reclogin"  class="nav-link ">Recivers</a></li>
      <li class="nav-item"><a href="/"   class="nav-link " >Home</a></li>
      <li class="nav-item"><a href="/adminlogin"  class="nav-link ">Admin</a></li>
    </ul>
  </header>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="quote-container animate__animated animate__fadeIn">
      <div class="quote">
        "Waste is not waste until we waste it. Join us in the fight against food waste and help nourish communities."
      </div>
      <div class="author">- ZeroWaste Team</div>

      <div class="login-options">
        <a href="/registrationpg" class="login-card donor animate__animated animate__fadeInLeft animate__delay-1s">
          <div class="login-icon">
            <i class="fas fa-hand-holding-heart"></i>
          </div>
          <div class="login-title">Donor Login</div>
          <div class="login-desc">Restaurants, caterers & households with surplus food</div>
        </a>

        <a href="/recreg" class="login-card receiver animate__animated animate__fadeInRight animate__delay-1s">
          <div class="login-icon">
            <i class="fas fa-utensils"></i>
          </div>
          <div class="login-title">Receiver Login</div>
          <div class="login-desc">NGOs, shelters & individuals in need of food</div>
        </a>
      </div>
    </div>
  </section>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const loginCards = document.querySelectorAll('.login-card');
      loginCards.forEach(card => {
        card.addEventListener('mouseenter', function () {
          this.classList.add('animate__pulse');
        });
        card.addEventListener('mouseleave', function () {
          this.classList.remove('animate__pulse');
        });
      });
    });
  </script>
</body>
</html>
